/**
 * Onboarding wizard — standalone entry point.
 *
 * The live route handlers and wizard HTML are implemented in
 * apps/api-server/src/routes/onboarding.ts and served at /onboarding
 * by the API server.
 *
 * This package is reserved for any future standalone onboarding server
 * (e.g. a separate Lightsail service). Nothing to run here yet.
 */
export {};
